# 更新日志

本项目所有重要变更都记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.0] - 2026-09-13

### 新增
- **设置页面**：全新设置页，卡片式布局，包含提醒设置和数据备份两大模块
- **到期提醒**：文档可设置到期日期，首页顶部显示即将到期的文档列表，支持自定义提醒天数（快速选择 7/15/30/90 天）
- **回收站**：删除的文档移入回收站，支持恢复和永久删除
- **排序功能**：首页支持按创建时间/更新时间排序，升序/倒序切换，默认创建时间倒序，切换筛选时保持排序状态
- **数据导出**：
  - 整站备份（数据库 + 所有图片打包下载）
  - 按时间段导出图片
  - 按标签导出图片（多选，OR 逻辑）
  - 按关键字导出图片（先查询匹配数量，再下载）
  - 三种导出方式 Tab 切换，界面简洁
- **图片打包下载**：详情页可将当前文档的所有图片打包为 ZIP 下载
- **编辑器全屏**：工具栏支持全屏编写，ESC 退出
- **链接/图片对话框**：编辑器插入链接和图片改为对话框形式，图片仅支持网络地址（http/https）手动输入
- **快捷键**：首页按数字键盘 `+` 快速跳转到添加页
- **统计概览**：首页显示文档总数、图片总数等统计信息

### 优化
- **设置页重新设计**：圆角方形图标 + 标题描述 + 控件区域，统一黑灰色图标风格
- **标签筛选逻辑**：从 AND 改为 OR（包含任一所选标签即显示）
- **首页搜索栏**：电脑端搜索框+分类+按钮同一行，手机端每个元素单独一行
- **导出 Tab 切换**：时间段/标签/关键字三种导出方式合并为一个卡片，Tab 切换
- **提醒快速选择**：7天/15天/30天/90天一键填入
- **CSS 结构优化**：设置页手机端样式移至文件末尾，确保正确覆盖电脑端样式，去除不必要的 `!important`
- **首页标题图标**：加大至 24px，视觉更协调
- **到期提醒标题**：过长自动截取并加 `title` 提示完整内容

### 移除
- **图片旋转功能**：移除编辑器中的图片旋转按钮和 `rotate_image.php`

### 修复
- **手机端设置页排版**：时间段导出按钮换行、标签列表左对齐、按钮宽度自适应
- **CSS 优先级问题**：手机端媒体查询被电脑端样式覆盖，调整 CSS 顺序后修复
- **分页器手机端换行**：优化分页器布局，避免挤成两行
- **标签空值显示**：修复空标签显示异常问题

## [1.2.0] - 2026-08-24

### 新增
- **草稿自动保存**（仅添加页）：输入内容和上传图片时自动保存到浏览器 localStorage，意外关闭页面后下次打开可恢复；选择「不恢复」时自动清理已上传但未使用的图片；提交成功后自动清除草稿
- **Markdown 工具栏**（电脑端）：粗体、斜体、下划线、高亮、标题、引用、行内代码、代码块、无序列表、有序列表、表格、链接、图片、上标、下标、分割线、语法帮助，共 18 个快捷按钮
- **标题下拉菜单**：点击 H 按钮展开，可选标题 1 / 标题 2 / 标题 3；当前行已是标题时自动替换级别
- **列表多行批量转换**：选中多行文字后点击列表按钮，每一行自动加上列表前缀
- **代码块智能包裹**：选中文字点代码块按钮自动包裹；未选中则插入代码块模板
- **分类提示填入**：首页选择分类后点击「添加」，添加页分类输入框下方显示「首页当前分类：XX [点击填入]」，用户自行输入时提示自动消失
- **CHANGELOG.md** 更新日志文件

### 优化
- **代码性能优化**：删除 config.php 中旧数据迁移代码（每次请求执行 PRAGMA table_info），删除未使用的 `upload_multiple_images` 和 `get_first_image` 函数
- **Markdown 解析器优化**：警告框 5 种类型映射移到类静态属性，避免每次解析重复构建数组
- **首页优化**：删除重复的 `$offset` 计算，所有 emoji 替换为 SVG 图标
- **编辑器体验**：电脑端显示工具栏，手机端隐藏工具栏并保留底部语法提示
- **详情页单图**：单张图片宽度 100%，不保持正方形
- **README.md**：全面更新功能说明，与当前代码保持一致
- 精简各文件多余注释

### 修复
- **Ctrl+Enter 提交不清除草稿**：`form.submit()` 程序化提交不触发 `submit` 事件，导致草稿残留，已在快捷提交前显式调用清除
- **编辑页误读草稿**：编辑页已有原始数据，不应读取/保存草稿，已限制草稿功能仅在添加页生效

## [1.1.0] - 2026-08-23

### 新增
- Markdown 警告框（GitHub 风格）：注意、提示、重要、警告、危险，5 种类型带 SVG 图标
- Markdown 扩展语法：下划线 `++`、高亮 `==`、上标 `^`、下标 `~`
- Markdown 嵌套列表：支持无序/有序混合嵌套递归解析
- Markdown 语法帮助页：15 个示例卡片，含代码和实时渲染效果
- 项目结构重构：CSS 迁入 `css/` 目录，JS 拆分为 `common.js`、`editor.js`、`viewer.js`
- 详情页更多菜单增加「返回首页」
- 网站图标 favicon.ico / favicon.svg

### 优化
- Header 统一样式：白底圆角 + 轻微阴影，标题居中，返回/更多按钮绝对定位
- 详情页图片默认显示 6 张（2 和 3 公倍数），超过显示「展开全部」
- 首页标签多选 AND 筛选，分类切换自动提交并保留搜索词和标签
- 分页器支持首页/末页快速跳转和指定页码输入
- 搜索结果关键词高亮
- 首页卡片仅标题可点击，避免误触

### 修复
- 标签乱码：数据库编码问题，增加 UTF-8 检测和自动转换
- 自动链接二次替换：URL 被嵌套在 `<a>` 标签内，增加负向断言修复
- 帮助页三级标题误加蓝色竖线：CSS 选择器收窄为直接子元素
- 电脑端 8 张图片最后一行空位：默认显示改为 6 张

## [1.0.0] - 2026-08-22

### 新增
- 文档增删改查基础功能
- 多图片上传与拖拽排序（支持手机端触摸）
- Markdown 编辑器双模式（编辑/预览）
- 分类、标签管理
- 搜索（标题+内容）
- 图片查看器（电脑端缩放拖拽，手机端新窗口）
- 响应式布局适配
- SQLite 数据库（WAL 模式 + 索引）
- CSRF 防护
